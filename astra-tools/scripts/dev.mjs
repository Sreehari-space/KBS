import {localStorage} from './revision-local.mjs';
const storage=localStorage();
import http from 'node:http';import fs from 'node:fs';import path from 'node:path';import worker from '../server/index.js';
const root=path.resolve('public');const types={'.html':'text/html; charset=utf-8','.css':'text/css','.js':'application/javascript','.svg':'image/svg+xml','.png':'image/png','.webp':'image/webp','.woff2':'font/woff2','.zip':'application/zip','.txt':'text/plain'};
const settings=fs.existsSync('.dev.vars')?JSON.parse(fs.readFileSync('.dev.vars','utf8')):{};
const assets={async fetch(request){let pathname;try{pathname=decodeURIComponent(new URL(request.url).pathname);}catch{return new Response('Bad path',{status:400});}const file=path.resolve(root,'.'+(pathname==='/'?'/index.html':pathname));if(!file.startsWith(root+path.sep))return new Response('Forbidden',{status:403});if(!fs.existsSync(file)||!fs.statSync(file).isFile())return new Response('Not found',{status:404});return new Response(fs.readFileSync(file),{headers:{'Content-Type':types[path.extname(file)]||'application/octet-stream'}});}};
const server=http.createServer(async(req,res)=>{try{const buffers=[];let size=0;for await(const b of req){size+=b.length;if(size>1500000){res.writeHead(413);res.end('Too large');return;}buffers.push(b);}const url='http://127.0.0.1:4173'+req.url;const request=new Request(url,{method:req.method,headers:{...req.headers,"oai-authenticated-user-id":"local-preview-owner"},...(['GET','HEAD'].includes(req.method)?{}:{body:Buffer.concat(buffers)})});const response=await worker.fetch(request,{...settings,...storage,ASSETS:assets});res.writeHead(response.status,Object.fromEntries(response.headers));res.end(Buffer.from(await response.arrayBuffer()));}catch{res.writeHead(500);res.end('The local server could not complete the request.');}});
server.listen(4173,'127.0.0.1',()=>console.log('Local: http://127.0.0.1:4173/'));for(const signal of ['SIGINT','SIGTERM'])process.on(signal,()=>server.close(()=>process.exit(0)));
