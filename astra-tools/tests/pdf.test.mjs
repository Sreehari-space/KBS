import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import vm from 'node:vm';
test('bundled PDF engine preserves receipt pages in a generated claim',async()=>{const context={Uint8Array,ArrayBuffer,TextEncoder,TextDecoder,setTimeout,clearTimeout,console};vm.createContext(context);vm.runInContext(fs.readFileSync('public/vendor/pdf-lib.min.js','utf8'),context);const {PDFDocument}=context.PDFLib;const source=await PDFDocument.create();source.addPage();source.addPage();const claim=await PDFDocument.create();claim.addPage();const pages=await claim.copyPages(source,source.getPageIndices());pages.forEach(p=>claim.addPage(p));const bytes=await claim.save();const parsed=await PDFDocument.load(bytes);assert.equal(parsed.getPageCount(),3);});
