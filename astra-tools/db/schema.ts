
import {sqliteTable,text,integer,index} from 'drizzle-orm/sqlite-core';
export const projects=sqliteTable('revision_projects',{id:text('id').primaryKey(),owner:text('owner').notNull(),payload:text('payload').notNull(),version:integer('version').notNull().default(1),archived:integer('archived').notNull().default(0),created:integer('created').notNull(),updated:integer('updated').notNull()},t=>[index('idx_revision_projects_owner').on(t.owner)]);
export const assets=sqliteTable('revision_assets',{id:text('id').primaryKey(),owner:text('owner').notNull(),project:text('project').notNull(),objectKey:text('object_key').notNull(),bytes:integer('bytes').notNull(),created:integer('created').notNull()},t=>[index('idx_revision_assets_owner_project').on(t.owner,t.project)]);
export const reviews=sqliteTable('revision_reviews',{id:text('id').primaryKey(),owner:text('owner').notNull(),project:text('project').notNull(),tokenHash:text('token_hash').notNull(),digest:text('digest').notNull(),objectKey:text('object_key').notNull(),requestIds:text('request_ids').notNull(),status:text('status').notNull().default('pending'),expires:integer('expires').notNull(),created:integer('created').notNull(),name:text('name'),note:text('note'),decided:integer('decided')},t=>[index('idx_revision_reviews_owner_project').on(t.owner,t.project),index('idx_revision_reviews_owner_created').on(t.owner,t.created)]);
export const usage=sqliteTable('revision_usage',{id:text('id').primaryKey(),count:integer('count').notNull().default(0)});
